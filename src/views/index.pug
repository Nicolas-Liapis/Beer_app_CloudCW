extends layout.pug

block content
  section#content
    h2 Welcome, #{username}!
    form(action='/goodbye', method='post')
      button(type='submit') Goodbye
    form(action='/beers', method='post')
      button(type='submit') Show be beers
